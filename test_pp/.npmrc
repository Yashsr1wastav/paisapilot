# Prevent npm from walking up to parent directories to find workspace roots.
# This stops Next.js from detecting C:\Users\yashr\package-lock.json as the
# workspace root, which caused the 14-second cold-start and the multiple-lockfiles warning.
workspaces-update=false
